# Logs Directory

Runtime log files are written here. The application config creates separate files for operator logs, audit logs, LLM activity, and verification/test helpers. Keep this directory on the NVMe-backed filesystem in deployment.
