# State Directory

SQLite databases, lock files, and restart-recovery artefacts live here. The main database uses WAL mode and is safe to keep on NVMe for lower write latency and better durability characteristics.
